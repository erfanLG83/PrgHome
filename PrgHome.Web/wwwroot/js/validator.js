//const validator = {
//    isEmpty: (text) => {
//        if (text.length === 0 || text === "")
//            return true;
//        else
//            return false;
//    }
//}
//const inputs = document.querySelectorAll('*[data-val]');
//inputs.forEach(elem => {
//    if (elem.attributes["data-val-required"]) {
//        elem.addEventListener('click')
//    }
//})